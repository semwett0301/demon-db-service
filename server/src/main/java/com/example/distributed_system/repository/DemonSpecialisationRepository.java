package com.example.distributed_system.repository;

import com.example.distributed_system.entity.DemonSpecialisation;
import org.springframework.data.jpa.repository.JpaRepository;

public interface DemonSpecialisationRepository extends JpaRepository<DemonSpecialisation, Integer> {
}