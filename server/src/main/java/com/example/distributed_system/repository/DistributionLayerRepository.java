package com.example.distributed_system.repository;

import com.example.distributed_system.entity.DistributionLayer;
import org.springframework.data.jpa.repository.JpaRepository;

public interface DistributionLayerRepository extends JpaRepository<DistributionLayer, Integer> {
}